using System;
using System.Collections.Generic;
using UnityEngine;

namespace ESGI.WFC.ThreeDimensions
{
    /*
    public class InfiniteMap : AbstractMap
    {
        public InfiniteMap(int mapHeight) : base()
        {
            Height = mapHeight;
            slots = new Dictionary<Vector3Int, Slot>();
            defaultColumn = new TilingMap(new Vector3Int(1, mapHeight, 1));
            if (ModuleData.currentModules == null || ModuleData.currentModules.Length == 0) {
                throw new InvalidOperationException("Module data was not available, please create module data first.");
            }
        }
    }*/
}